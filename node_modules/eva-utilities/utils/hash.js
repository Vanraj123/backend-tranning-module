const crypto = require('crypto');

module.exports = function makeCreateHash() {
  return function createHash({ data, secret, algorithm = 'sha256' }) {
    const hash = crypto.createHash(algorithm, secret);
    hash.update(data);
    return hash.digest('hex');
  }
}